{
  "name": "me/geoblocker",
  "description": "GeoBlocker dependencies",
  "type": "project",
  "require": {},
  "require-dev": {
    "phpunit/phpunit": "^9.0"
  },
  "scripts": {
        "lint": "find . -name \\*.php -not -path './vendor/*' -print0 | xargs -0 -n1 php -l",
        "cs:check": "php-cs-fixer fix --dry-run --diff"
    }
}
